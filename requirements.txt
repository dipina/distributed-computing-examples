# Core examples (01-09, 14, 16-23) need ONLY the Python standard library (Python >= 3.10).
# The packages below enable the remaining examples; missing ones are SKIPPED, not failed.

# 10 REST, 11 GraphQL, 13 real-time web, 15 microservices, 24 capstone (needs all of these)
fastapi>=0.110
uvicorn>=0.29
httpx>=0.27
# 11 GraphQL
strawberry-graphql>=0.230
# 12 gRPC
grpcio>=1.62
grpcio-tools>=1.62
# 13 WebSocket client
websockets>=12.0

# ---- optional extras (their parts are skipped gracefully if absent) ----
# 14 real RabbitMQ part (also needs a running broker, see README)
pika>=1.3
# 23 interoperability test with the official MCP SDK
mcp>=1.10          # 1.x (FastMCP) and 2.x (MCPServer) both supported
# 20 bonus part on the real Ray runtime (large download, ~80 MB)
# ray>=2.9

# tests
pytest>=8.0
